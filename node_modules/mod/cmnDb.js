/**************************************************************************************************
 * モジュール名　: DB接続
 * 概要　　　　　: MongoDBに接続する
 * 作成日　　　　: 2014/03/05
 * 更新日　　　　: yyyy/MM/dd
 *************************************************************************************************/

// モジュールインポート
var mongodb = require("mongodb");

// MongoServerオブジェクト作成。接続情報は後でConfigに
var dbServer = new mongodb.Server("localhost", 27017);

// データベースオブジェクト作成（データベース名/MongoServerオブジェクト/オプション）
// safe=trueはデータ更新時に完了を通知する設定
var db = new mongodb.Db("comicManager", dbServer, {safe : true});

// エクスポート
module.exports = db;